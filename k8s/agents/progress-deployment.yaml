apiVersion: apps/v1
kind: Deployment
metadata:
  name: progress-agent
  namespace: emberlearn
  labels:
    app: progress-agent
    component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: progress-agent
  template:
    metadata:
      labels:
        app: progress-agent
        component: backend
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "progress-agent"
        dapr.io/app-port: "8006"
        dapr.io/enable-api-logging: "true"
    spec:
      containers:
        - name: progress-agent
          image: emberlearn/progress-agent:latest
          ports:
            - containerPort: 8006
          env:
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: emberlearn-secrets
                  key: openai-api-key
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: emberlearn-secrets
                  key: database-url
            - name: ENVIRONMENT
              value: "production"
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /ready
              port: 8006
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: progress-agent
  namespace: emberlearn
spec:
  selector:
    app: progress-agent
  ports:
    - port: 8006
      targetPort: 8006
  type: ClusterIP
