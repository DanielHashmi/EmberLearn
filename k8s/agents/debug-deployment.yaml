apiVersion: apps/v1
kind: Deployment
metadata:
  name: debug-agent
  namespace: emberlearn
  labels:
    app: debug-agent
    component: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: debug-agent
  template:
    metadata:
      labels:
        app: debug-agent
        component: backend
      annotations:
        dapr.io/enabled: "true"
        dapr.io/app-id: "debug-agent"
        dapr.io/app-port: "8004"
        dapr.io/enable-api-logging: "true"
    spec:
      containers:
        - name: debug-agent
          image: emberlearn/debug-agent:latest
          ports:
            - containerPort: 8004
          env:
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: emberlearn-secrets
                  key: openai-api-key
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: emberlearn-secrets
                  key: database-url
            - name: ENVIRONMENT
              value: "production"
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8004
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /ready
              port: 8004
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: debug-agent
  namespace: emberlearn
spec:
  selector:
    app: debug-agent
  ports:
    - port: 8004
      targetPort: 8004
  type: ClusterIP
