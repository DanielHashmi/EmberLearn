apiVersion: apps/v1
kind: Deployment
metadata:
  name: code-review-agent
  namespace: default
  labels:
    app: code-review-agent
spec:
  replicas: 2
  selector:
    matchLabels:
      app: code-review-agent
  template:
    metadata:
      labels:
        app: code-review-agent
      annotations:
        dapr.io/enabled: 'true'
        dapr.io/app-id: code-review-agent
        dapr.io/app-port: '8002'
        dapr.io/log-level: info
    spec:
      containers:
      - name: code-review-agent
        image: emberlearn/code-review-agent:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8002
          name: http
        env:
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: openai-secret
              key: api-key
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /ready
            port: 8002
          initialDelaySeconds: 10
          periodSeconds: 5
