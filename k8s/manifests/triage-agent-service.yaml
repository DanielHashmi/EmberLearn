apiVersion: v1
kind: Service
metadata:
  name: triage-agent-service
  namespace: default
  labels:
    app: triage-agent
spec:
  type: ClusterIP
  selector:
    app: triage-agent
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
