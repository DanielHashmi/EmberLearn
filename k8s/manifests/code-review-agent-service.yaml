apiVersion: v1
kind: Service
metadata:
  name: code-review-agent-service
  namespace: default
  labels:
    app: code-review-agent
spec:
  type: ClusterIP
  selector:
    app: code-review-agent
  ports:
  - port: 80
    targetPort: 8002
    protocol: TCP
    name: http
