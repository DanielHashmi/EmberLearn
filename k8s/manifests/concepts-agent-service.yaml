apiVersion: v1
kind: Service
metadata:
  name: concepts-agent-service
  namespace: default
  labels:
    app: concepts-agent
spec:
  type: ClusterIP
  selector:
    app: concepts-agent
  ports:
  - port: 80
    targetPort: 8001
    protocol: TCP
    name: http
