apiVersion: v1
kind: Service
metadata:
  name: debug-agent-service
  namespace: default
  labels:
    app: debug-agent
spec:
  type: ClusterIP
  selector:
    app: debug-agent
  ports:
  - port: 80
    targetPort: 8003
    protocol: TCP
    name: http
