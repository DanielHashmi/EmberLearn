[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
asyncio_mode = auto
addopts = -v --tb=short

# Hypothesis settings for CI
[hypothesis]
max_examples = 100
deadline = 5000
